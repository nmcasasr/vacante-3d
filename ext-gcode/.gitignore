.DS_Store
node_modules/
*.vsix

# OrcaSlicer's CLI drops this in whatever directory it runs from.
result.json

# vacante-g-code is its OWN repository (github.com/nmcasasr/vacante-g-code).
# Without this, `git add` here records it as a bare gitlink — a half-submodule
# that clones as an empty directory. Keep the two repos separate.
vacante-g-code/
